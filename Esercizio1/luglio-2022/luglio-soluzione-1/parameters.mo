
record Prm

parameter Integer ID = 1234567;  //  Sostituire qui la vostra matricola

parameter Integer MyMagicNumber = 1 + mod(ID, 173);  

parameter Integer N = 5;  // numero di fasi del ciclo di sviluppo

parameter Integer W = 5;  // numero di development teams in azienda


end Prm;

